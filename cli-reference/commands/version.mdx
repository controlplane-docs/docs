---
title: version
description: "cpln version — Print the installed cpln version and build information."
keywords: ['cpln version', 'cli version', 'version check', 'build info', 'cpln --version', 'which version']
---

Print the installed cpln version



**Usage**

```none
cpln version [OPTIONS]
```

<Note>

`cpln version` and its short form `cpln -v` print the same information as `cpln --version`.

</Note>


<Accordion title="Reference">

```text
Options:
  --help         Show help  [boolean]
  --version, -v  Show version number  [boolean]
```

[Common Options Reference](/cli-reference/using-cli/common-options)

</Accordion>


<Accordion title="Example">

<CodeGroup>

```bash Command
cpln version
```

```text Output
{
  "npm": "3.14.0",
  "build": "3.14.0",
  "version": "3.14.0",
  "epoch": 174,
  "timestamp": 1752710400000
}
```

</CodeGroup>


</Accordion>

---
