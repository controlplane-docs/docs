---
title: MCP Server
sidebarTitle: Overview
description: Connect AI assistants and tools to Control Plane using the Model Context Protocol (MCP)
keywords: ['natural language', 'llm', 'agent', 'integration', 'cpln mcp', 'ai infrastructure', 'tool calling', 'function calling', 'json rpc', 'devops automation']
---

The Control Plane MCP Server enables AI assistants and tools to interact directly with your Control Plane infrastructure. Using the [Model Context Protocol (MCP)](https://modelcontextprotocol.io/), you can create, manage, and deploy resources through natural language conversations.

<Card title="Use the AI Plugin for the best experience" icon="robot" href="/mcp/ai-plugin" horizontal>
  The **Control Plane AI Plugin** auto-configures this MCP server and adds 23 skills, 8 agents, 8 commands, and 8 guardrail rules that prevent the most common deployment mistakes. Recommended for Claude Code, Codex, and Gemini CLI users.
</Card>

## What is MCP?

The [Model Context Protocol (MCP)](https://modelcontextprotocol.io/) is an open standard that allows AI models to securely connect to external tools and data sources. With the Control Plane MCP Server, AI assistants can:

<CardGroup cols={2}>
  <Card title="Manage Resources" icon="cloud">
    Create and configure GVCs, workloads, and more
  </Card>
  <Card title="Deploy Applications" icon="rocket">
    Build images and deploy applications across global locations
  </Card>
  <Card title="Query Infrastructure" icon="magnifying-glass">
    Get real-time information about your Control Plane resources
  </Card>
  <Card title="Automate Workflows" icon="robot">
    Streamline DevOps tasks through conversational AI
  </Card>
</CardGroup>

## Quick Start

<Steps>
  <Step title="Create a Service Account">
    Create a [service account](/guides/create-service-account) with the appropriate permissions for your use case. Generate an API key that
    will be used for authentication.
    <Tip>
      For quick setup, add your service account to the **superusers** group for full access, or the **viewers** group for read-only access.
      See [Service Account Permissions](#service-account-permissions) below for details.
    </Tip>
  </Step>
  <Step title="Configure Your Client">
    Add the Control Plane MCP Server to your AI assistant or tool. See the [setup guides](#compatible-tools) below for tool-specific instructions.
  </Step>
  <Step title="Start Building">Begin interacting with Control Plane through natural language commands.</Step>
</Steps>

## MCP Server Endpoint

<ParamField path="url" type="string" required>
  ``` https://mcp.cpln.io/mcp ```
</ParamField>

## Authentication

The MCP Server requires authentication via a service account token. Include the token in the `Authorization` header:

```text
Authorization: <your-service-account-token>
```

<Warning>Keep your service account token secure. Never commit tokens to version control or share them publicly.</Warning>

## Compatible Tools

The Control Plane MCP Server works with **any AI assistant or development tool that supports remote MCP servers**. Below are setup guides for popular tools:

<CardGroup cols={3}>
  <Card title="Antigravity" href="/mcp/antigravity">
    Google Antigravity IDE
  </Card>
  <Card title="Claude Code" href="/mcp/claude">
    Claude Code CLI
  </Card>
  <Card title="Codex" href="/mcp/codex">
    OpenAI Codex
  </Card>
  <Card title="Cursor" href="/mcp/cursor">
    Cursor IDE
  </Card>
  <Card title="Gemini CLI" href="/mcp/geminicli">
    Gemini CLI
  </Card>
  <Card title="VS Code" href="/mcp/vscode">
    Visual Studio Code
  </Card>
</CardGroup>

<Note>
  Don't see your tool listed? Any client that supports remote MCP servers can connect to the Control Plane MCP Server using the standard configuration format. Check your tool's documentation for remote MCP server setup instructions.
</Note>

## Service Account Permissions

The MCP Server respects all Control Plane [policies](/reference/policy) and permissions. Your service account's capabilities depend on the groups and policies assigned to it.

### Built-in Groups

Control Plane provides two built-in groups for common permission patterns:

| Group          | Permissions               | Use Case                                              |
| -------------- | ------------------------- | ----------------------------------------------------- |
| **viewers**    | `view` on all resources   | Read-only exploration, querying infrastructure status |
| **superusers** | `manage` on all resources | Full automation, creating and modifying all resources |

### Custom Permissions

For more granular control, you can:

1. Create custom [groups](/reference/group) with specific members
2. Define [policies](/reference/policy) that grant targeted permissions
3. Assign your service account to groups that match your use case

<Tip>
  Create dedicated service accounts for different environments (development, staging, production) with appropriate permission scopes. For
  detailed information on permissions and policies, see the [Policy Reference](/reference/policy).
</Tip>

### Recommended Approaches

| Use Case                | Recommended Setup                                                 |
| ----------------------- | ----------------------------------------------------------------- |
| Quick exploration       | Add service account to **viewers** group                          |
| Full development access | Add service account to **superusers** group                       |
| Production automation   | Create custom group with specific policies for required resources |
| Team-specific access    | Create group per team with policies scoped to their GVCs          |

## How It Works

When an AI assistant connects to the MCP Server, it follows this initialization flow:

<Steps>
  <Step title="Read the Guide">
    The assistant reads the `cpln+virtual://guide` resource to understand available tools, naming conventions, and best practices.
  </Step>
  <Step title="Read Server Info">
    The assistant reads `cpln+virtual://about` to see server version, available tools, resources, and prompts.
  </Step>
  <Step title="Set Context">
    The assistant calls `set_context` to establish the default org and GVC for the session.
  </Step>
  <Step title="Begin Operations">
    The assistant can now use any of the 55+ tools to manage Control Plane resources.
  </Step>
</Steps>

## Available Resources

The MCP Server provides these resources for AI assistants to load context:

| Resource | URI | Description |
|----------|-----|-------------|
| Guide | `cpln+virtual://guide` | MCP usage guide with tool taxonomy and best practices |
| About | `cpln+virtual://about` | Server metadata, version, and capability summary |
| CLI Guide | `cpln+virtual://cli/guide` | CLI reference for `cpln` commands |
| Canon | `cpln+virtual://canon` | CLI patterns, API canon, and guardrails |
| Docs (Concise) | `cpln+virtual://docs/concise` | LLM-friendly platform overview |
| Docs (Full) | `cpln+virtual://docs/full` | Complete documentation reference |
| OpenAPI | `cpln+virtual://openapi` | Control Plane OpenAPI specification |
| Templates | `cpln+virtual://templates/{kind}` | Quick-start resource templates |

## Available Prompts

The MCP Server includes curated prompts for common workflows:

| Prompt | Description |
|--------|-------------|
| General Assistant | Overall platform guidance and operations |
| GVC Operations | GVC creation, management, and location configuration |
| Workload Deployment | Container deployment, scaling, and configuration |
| Secret Patterns | Secret injection and workload access patterns |
| Troubleshooting | Debugging workload issues and failures |
| Image Management | Container image registry operations |
| Expert Assistant | Advanced platform engineering scenarios |
| Platform Engineer | Infrastructure design and architecture |

## Best Practices

<CardGroup cols={2}>
  <Card title="Start with Context" icon="bullseye">
    Always set your org and GVC context at the start of a conversation
  </Card>
  <Card title="Be Specific" icon="list-check">
    Provide specific values for resources (memory, CPU, replicas) when possible
  </Card>
  <Card title="Confirm Destructive Actions" icon="triangle-exclamation">
    Configure your AI assistant to confirm before executing delete or update operations
  </Card>
  <Card title="Use Tags" icon="tags">
    Add tags to resources for better organization and filtering
  </Card>
</CardGroup>

### Recommended Prompts Structure

For the best results, structure your prompts with:

1. **Context**: Org and GVC
2. **Action**: What you want to do
3. **Target**: Which resource
4. **Details**: Specific configuration values

<CodeGroup>
```text Good Prompt
Using org "my-org" and GVC "production", create a workload called "api-v2" 
with image nginx:latest, 512Mi memory, port 8080, and minimum 2 replicas.
```

```text Less Effective
Make a new workload.
```

</CodeGroup>

## Next Steps

<CardGroup cols={2}>
  <Card title="Setup Guide" icon="gear" href="/mcp/claude">
    Configure the MCP Server for your preferred tool
  </Card>
  <Card title="AI Plugin" icon="puzzle-piece" href="/mcp/ai-plugin">
    Skills, agents, and commands for AI tools
  </Card>
  <Card title="Usage Examples" icon="lightbulb" href="/mcp/examples">
    See practical examples of what you can build
  </Card>
  <Card title="Create Service Account" icon="key" href="/guides/create-service-account">
    Generate a token for MCP Server authentication
  </Card>
</CardGroup>
