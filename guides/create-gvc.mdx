---
title: Create a GVC
description: "Step-by-step guide to create Global Virtual Clouds via Console UI and CLI. Covers naming, cloud provider selection, pull secrets, and tags."
keywords: ['cpln gvc', 'new gvc', 'gvc creation', 'environment setup', 'multi-region setup', 'aws gcp azure', 'pull secret config', 'gvc manifest', 'first deployment env']
---

## GVC Creation Overview

Follow the steps below to create a [Global Virtual Cloud (GVC)](/reference/gvc) within your [org](/reference/org).

## GVC Creation Prerequisites

- Review the [GVC](/reference/gvc) reference page
- [Permissions](/reference/policy#permissions) to create a [GVC](/reference/gvc)
- Optional:
  - Install the [CLI](/cli-reference/installation)

## Create using the UI Console

Follow the steps below to create a GVC:

1. Create a new GVC using one of the following methods:

   - For an [org](/reference/org) without any GVCs:
     - After logging in, the `Create GVC` page will be shown, or if you browse to a different page,
     - Click the `Create` dropdown in the upper right corner and select `GVC`
   - For an [org](/reference/org) with at least one GVC:
     - Click the `Create` dropdown in the upper right corner and select `GVC`

2. Enter a unique name and optional description.
3. Select `Locations` from the left pane. Choose at least one cloud provider location.
4. Select `Pull Secrets` from the left pane. If any [Docker/ECR/GCP Pull Secrets](/reference/gvc#pull-secrets) are configured for your [org](/reference/org), select the ones required by your [workloads](/reference/workload) to authenticate to a private registry.
5. Optionally, select `Tags` from the left pane to add [tags](/core/misc#tags).
6. Click `Create`.

Your GVC has been successfully created and is ready to be configured with a [workload](/reference/workload).

## Create using the CLI

Refer to the [GVC create](/cli-reference/commands/gvc#gvc-create) command for details and examples on how to create a GVC using the CLI.
